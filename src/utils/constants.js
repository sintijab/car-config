// endpoints
const EP_MAKES = '/makes'
const EP_MODELS = '/models'
const EP_VEHICLES = '/vehicles'

export const endpoints = { EP_MAKES, EP_MODELS, EP_VEHICLES }

export const filters = { MODEL: 'models', VERSION: 'versions', VEHICLE: 'vehicles' }

export const navItems = { MODELS: 'Models', VERSIONS: 'Versions', VEHICLES: 'Vehicles' }

export const textMessage = { ERROR: 'Service unavailable', NOT_AVAILABLE: 'No available' }

export const PAGINATION_AMOUNT = 10
